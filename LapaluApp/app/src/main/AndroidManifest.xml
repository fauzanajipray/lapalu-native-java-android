<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faprayyy.lapaluapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgotPassword_2"></activity>
        <activity android:name=".ForgotPassword_1" />
        <activity android:name=".Home" />
        <activity android:name=".SignUpSuccess" />
        <activity android:name=".signUp" />
        <activity android:name=".Login" />
        <activity android:name=".Welcome_3" />
        <activity android:name=".Welcome_2" />
        <activity android:name=".Welcome_1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>